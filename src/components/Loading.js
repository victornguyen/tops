import React from 'react'
import styled from 'styled-components'

const Container = styled.div`
  border: 1px solid #e1e1e1;
  padding: 10em;
  color: #999;
  text-align: center;
`

const Loading = () => <Container>Loading products...</Container>

export default Loading
